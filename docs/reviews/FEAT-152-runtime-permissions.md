# FEAT-152 current delivery status — 2026-09-08

The local demo_fast implementation and S1–S4/D4 acceptance passed on 2026-09-07. Real configured-native-policy denial, exact-action native manual approval, same-auto-task continuation, full access, ask contraction and normal recovery are recorded in the feature package. The original safety policy was preserved; the temporary review requirement has been removed. This is not evidence of default-policy natural risk denial. Codex core and retained binary remain unchanged.

The user now requests a scoped four-repository commit sequence and pinned local delivery. This contract family is 0.1.0, independent of the preserved v1–v6 families. The Contracts commit must be created first after explicit commit authorization; Host will pin that actual full commit and generated source digests, Desktop will pin the actual Contracts and Host commits, and the feature package will record all resulting commits. No nonexistent commit or released tag is claimed. Current source family/generator bytes are unchanged from the passed contract conformance checks.

Latest local UI/entrypoint follow-up changes are validated separately before delivery. Public/production activation, push and tagging remain outside this request. Legacy v4/Skills/Runtime locks and FEAT-137 retirement remain intact. The history below preserves each stage's conclusions at the time and does not describe the current completion state.

---

# FEAT-152 source-first review

Contract impact: semantic at the new Desktop default; additive HTTP extension with legacy v1-v6 byte/behavior preservation. Local demo only; no published release claim.

Authority: retained FEAT-136 binary b2b20e2fc4a0 and its canonical schema; upstream rust-v0.144.6 supplies approvalPolicy, approvalsReviewer and sandboxPolicy plus native command/file/permission approval callbacks, autoApprovalReview notifications and thread/approveGuardianDeniedAction. Do not rely on retired v6 provenance or deterministic producer.

Owner/consumer: 段成威; producer Agent Host; consumer Desktop native. User approved three modes and interaction rules in the current conversation on 2026-09-06.

Install provider support before enabling the new native consumer. The new request explicitly includes mode in operation identity. Legacy callers do not issue it and retain read-only/never. Native stores mode atomically with task creation; no mode changes during queued/active/pending approvals. Host forwards only exact live callbacks, keeps Runtime identifiers private, and fails closed after normal cancellation or connection end. Raw payloads remain memory-only; UI strings are bounded and redacted. Scope is enforced through existing native session owner and Host bearer.

Auto review policy remains Codex-owned; only a Runtime denied assessment can be manually approved through the native upstream operation. No model policy replication. Approve once never amends persistent execution/network rules. Request permissions grants are limited to the turn.

S1 focused generation and spec lint PASS; contract tests 3/3 PASS, including observations from the real retained Runtime. Native storage/schema tests 2/2 and default-off UI checks 67/67 PASS. Real turn execution and recovery, all feature AC, and immutable publishing pins remain NOT RUN. Generation uses current sibling sources for local candidate verification. Old approval retirement stays in force.

The user subsequently required staged delivery and explicitly prohibited Codex core changes. S1, S2 and the user-ordered S3 checks are completed; S4 and whole-feature acceptance remain pending and the new feature is disabled by default at Host, Native, and UI boundaries. Runtime core and binary hashes are unchanged. Zero-turn thread/resume returned no rollout found in the initial real check; that limitation is preserved and real recovery is reserved for a completed-turn test.


S2 (2026-09-07): the real Desktop → Host → retained Runtime → MiniMax path produced ordinary command approval and refusal. Approval wrote the expected scratch file only after the UI decision; refusal left the other file absent. Normal completed-thread resume was connected through the existing native resume path and verified by continuing the same task. Six of ten authorized provider requests were consumed. Native 2, Desktop 75, Contracts 3 and Go race/lint checks passed. Publication pins, automatic review/full execution and whole-feature D4 remain NOT RUN. Evidence: ../yijie/docs/features/FEAT-152-desktop-permission-modes/evidence/stage2/.


S3 (2026-09-07): Native guardian used MiniMax-M3/minimax, returned allow, and its structured Responses request was metered alongside the parent request and tool follow-up. First full-access cancel/confirm and same-task full → ask contraction passed with real Runtime turn context and ordinary files. Total paid requests: 13/15 after the user added 5 to the original 10. Desktop 77 scoped tests, Host race/lint and Contracts 3 checks passed. No public wire change was required in this stage. A naturally denied automatic review did not occur; manual takeover was reviewed against native inject_no_new_turn semantics but its real approval branch is NOT RUN. No fabricated Runtime callback or dangerous action was used. Core/retained Runtime unchanged; S4/D4 remain pending. Evidence: ../yijie/docs/features/FEAT-152-desktop-permission-modes/evidence/stage3/.
